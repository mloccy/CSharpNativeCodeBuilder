{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "command": "dotnet",
            "args": ["build"],
            "options": {
                "cwd": "${workspaceFolder}/NativeCodeBuilder/Artomatix.NativeCodeBuilder/"
            }
        }
    ]
}